services:
  backend:
    build: 
      - context: ./backend
    container_name: backend
    depends_on:
      - postgres-db

  postgres-db:
    image: postgres
    container_name: postgres
    restart: always
    volumes:
      - postgres-data:/var/lib/postgresql/data 

volumes:
 - postgres-data:
